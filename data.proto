syntax = "proto3";

message FileSystem {
  repeated File files = 1;
}

message File {
  string name = 1;
  uint32 perms = 2;
  oneof file {
    Directory dir = 3;
    Normal normal = 4;
  }
}

message Directory {
  repeated File files = 1;
}

message Normal {
  uint64 size = 1;
}
